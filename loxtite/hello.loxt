var hello = 1234.5;

fun goodbye(time) {
    return time > 100.0;
}

while (hello != 10000.0 and !goodbye(time)) {
    printf("Hello");
    hello = hello + 0.1;
}

/* This comment should be ignored */
